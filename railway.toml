[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python migrate.py && gunicorn wsgi:app --workers 1 --timeout 120 --bind 0.0.0.0:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
